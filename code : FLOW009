#include <stdio.h>

int main(void) {
	int T,n=0,x,y;
	double z=0.0;
	scanf("%d\n", &T);
	while(n<T) {
		scanf("%d %d\n", &x,&y);
		
		if (x>1000) {
			z = x*y*0.9; 
			printf("%f\n", z);
		}
		else {
			z = (x*y);
			printf("%f\n", z);
		}
		
		n++;
  }
  return 0;
}
    
