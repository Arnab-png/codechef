#include <stdio.h>

int main(void) {
	int T,n=0,x,y;
	double z;
	scanf("%d\n", &T);
	while(n<T) {
		scanf("%d %d\n", &x,&y);
		
		if (x>1000) {
			z = (x*y)-(x*y*0.01); 
			printf("%d\n", z);
		}
		else {
			z = (x*y);
			printf("%d\n", z);
		}
		
		n++;
  }
  return 0;
}
    
